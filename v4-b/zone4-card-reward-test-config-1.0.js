/* Zone 4 Card Reward test location: visible Row 6, Column 8 = zero-based 5,7. */
(()=>{
  const cfg=window.HAJJEN_ZONE_CONFIG;
  if(!cfg||cfg.zone!==4)return;
  cfg.cardRewardTest={row:5,col:7,title:'CARD REWARD'};
})();
