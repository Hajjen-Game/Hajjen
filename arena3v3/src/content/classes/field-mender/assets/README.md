# Animation assets

Place future character-specific animation files for this combatant in this folder.

Recommended future structure:

- `idle/`
- `move/`
- `cast/`
- `hit/`
- `death/`

The runtime config for this character lives one directory above in `config.js`.
